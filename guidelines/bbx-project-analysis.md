# BBX Website 项目分析

## 项目概述
BBX Website 是一个加密货币交易和信息平台，提供数字货币交易、市场数据分析、用户社区等功能。该项目采用现代化的前端技术栈构建，支持多语言，并集成了多种第三方服务和API。

## 技术栈

### 前端技术
1. **框架和库**：
   - Next.js 14.2.3（React 框架）
   - React 18.2.0
   - TypeScript
   - Tailwind CSS（用于样式）
   - next-intl（国际化）
   - SWR（数据获取）

2. **UI 组件**：
   - 自定义UI组件库（在 components/ui 目录下）
   - Radix UI 组件（如@radix-ui/react-accordion等）
   - Antd（Ant Design）
   - Material UI（MUI）
   - Framer Motion（动画）
   - Echarts（图表可视化）

3. **状态管理**：
   - React Context API
   - SWR

4. **身份认证**：
   - NextAuth
   - 支持多种登录方式（Telegram、Farcaster、Twitter等）
   - JWT 认证

### 后端技术
1. **API 路由**：
   - Next.js API Routes（在 app/api 目录下）
   - 外部API服务集成（如 Strapi 后端）

2. **数据存储**：
   - Redis
   - 可能使用外部 CMS（Strapi）

3. **服务集成**：
   - 加密货币相关 API
   - 区块链接口
   - 社交媒体 API（Twitter、Telegram、Farcaster）

### DevOps 和基础设施
1. **部署和CI/CD**：
   - Docker（有 Dockerfile）
   - GitLab CI/CD 配置（.gitlab-ci.yml）
   - Kubernetes 配置文件

2. **代码质量和风格**：
   - ESLint
   - Prettier
   - Husky（Git 钩子）
   - Conventional Commits

3. **测试**：
   - Jest
   - Playwright（E2E测试）

## 项目结构

### 核心目录
1. **app/**：
   - Next.js App Router 目录
   - 包含页面路由和API路由
   - 使用 [locale] 支持多语言

2. **components/**：
   - UI组件库
   - 业务组件（如 chart, account, dashboard 等）
   - 布局组件（layout）

3. **lib/**：
   - 公共工具函数
   - 服务集成

4. **hooks/**：
   - 自定义 React Hooks

5. **store/**：
   - 状态管理

6. **public/**：
   - 静态资源

7. **styles/**：
   - 全局样式

### 主要功能模块
1. **用户认证和账户**：
   - 多种登录方式（Telegram、Social、Web3）
   - 用户管理与设置

2. **交易功能**：
   - 订单系统（Orderly Network集成）
   - 交易记录和历史
   - 交易佣金系统

3. **市场数据和图表**：
   - 加密货币信息
   - 图表分析
   - 监视列表

4. **社区功能**：
   - Twitter 集成
   - Farcaster 集成
   - 社区讨论

5. **内容管理**：
   - 文章系统
   - 新闻服务

## 多语言支持
项目支持多语言，使用 next-intl 实现，主要语言路由位于 app/[locale] 目录下。

## 部署和环境
项目可使用 Docker 和 Kubernetes 部署，支持不同环境配置。

## 接口集成
项目集成了多种第三方服务和API：
1. 加密货币交易服务（如 Orderly Network）
2. 社交媒体（Twitter、Telegram、Farcaster）
3. 区块链服务（Solana、Optimism等）
4. CMS 和后端服务

## 详细组件结构

### UI组件库
项目有丰富的UI组件库，位于 components/ui 目录下，包括：
- 基础组件：按钮、输入框、选择器等
- 布局组件：卡片、导航菜单、侧边栏等
- 反馈组件：对话框、警告框、提示等
- 数据展示：表格、走势图、图表等

### 业务组件
业务组件按功能模块组织，主要包括：
- 账户相关（account）
- 图表相关（chart）
- 加密货币（currencies）
- 交易相关（orderly, dex）
- 仪表盘（dashboard-layout）
- 社区相关（community, twitter-feeds, warpcast）

## 路由结构
项目使用 Next.js App Router，主要路由结构：
- app/[locale]/(auth)/ - 认证相关页面
- app/[locale]/(dashboard)/ - 主要应用页面
- app/[locale]/(orderly)/ - 交易相关页面
- app/[locale]/(community)/ - 社区相关页面
- app/api/ - 后端API接口

## API接口
项目有大量API接口，主要分类：
- 用户认证相关（auth, user, account）
- 交易相关（trade, order）
- 市场数据（data, coins, cryptos）
- 社区功能（twitter, farcaster, telegram）
- 监视列表和提醒（watchlist, alerts） 

## 详细页面与区域/版块定义 (供AI助手选项使用)

**基于项目结构 `app/[locale]/` 及功能模块推断，为PM助手提供以下主要分组选项，对应表单第一步选择：**

---

**A. 主要分组：CEX - Market**
    *   **1. 页面：关注列表 (`cex_market_watchlist`)**
        *   **具体区域/版块：**
            *   **Crypto (自选币种)部分:**
                *   列表标题/管理入口区
                *   添加自选币种搜索/按钮区
                *   自选币种行情列表区 (包含#、币种、最新价、24H涨跌、成交量、市值、24H趋势图等列)
            *   **KOL (关注KOL动态)部分:**
                *   关注KOL的Farcaster Casts信息流区
                *   关注的KOL列表区 (可管理)
                *   KOL关注币种动态区 (例如"添加到加密货币"列表)
    *   **2. 页面：加密货币 (`cex_market_crypto`)**
        *   **具体区域/版块：**
            *   板块分类导航区 (例如"再质押"、"英国大选"、"BRC-20"等标签页)
            *   主行情列表筛选区 (含'全部'、'自选'标签页)
            *   搜索框 (通常存在)
            *   行情列表表头
            *   行情数据列表区域 (包含#、币种、最新价等)
            *   分页控件区域 (通常存在)
            *   热门币种展示区 (例如"热门"横向卡片列表)
            *   涨幅榜展示区 (例如"涨幅榜"横向卡片列表)
            *   新币速递展示区 (例如"新币"横向卡片列表)
    *   **3. 页面：合约交易 (`cex_market_futures`)**
        *   **具体区域/版块：** (对应原 `trading_futures`)
            *   合约信息头部
            *   持仓管理面板
            *   合约下单区域
            *   合约K线图
        *   **注意：合约交易页面通常高度复杂和集成，包含大量实时数据和交互元素。通过此简单表单进行的微调可能范围有限，复杂修改建议与开发团队直接沟通。**
    *   **4. 页面：ETF (`cex_market_etf`)**
        *   **具体区域/版块：**
            *   ETF主要分类标签页 (例如"美股BTC"、"美股ETH"、"港股BTC"、"港股ETH"等)
            *   ETF分类概览数据区 (显示所选分类的总净流入、总市值等)
            *   ETF资金流向时序数据表 (按日期和具体ETF产品展示净流入/流出)
            *   ETF资金流向可视化图表区 (例如展示各类ETF净流入的图表)
            *   ETF产品列表/市场数据表 (包含#、标的、净流入、价格、市值、费率、机构等列)
            *   ETF AI分析与解读侧边栏 (通过特定按钮触发，显示市场分析、数据解读、新闻摘要等)
            *   (可能存在)单个ETF详情核心展示区 (在点击列表项后，或作为页面一部分)
            *   (可能存在)申购/赎回操作区
    *   **5. 页面：交易所 (`cex_market_exchanges`)**
        *   **具体区域/版块：**
            *   交易所列表区 (包含#、交易所名称、成交量、体验金、币种数量、交易对等)
            *   **选中交易所详情展示区:**
                *   交易所基本信息与链接区 (Logo, 名称, 排名, 手续费, 官网, App链接, 社交链接等)
                *   交易所交易量数据区 (区分现货、衍生品)
                *   交易所介绍文本区
                *   交易所支持市场/交易对列表区 (含现货、衍生品筛选)
    *   **6. 页面：发现 (`cex_market_discover`)**
        *   **具体区域/版块：**
            *   KOL提及币种追踪区 (列表包含硬币、提及者、时间、价格、趋势、相关图片等)
            *   KOL影响力排行榜区 (列表包含影响力人物、关注者、提及币种、回报率、胜率等)
    *   **7. 页面：快讯 (`cex_market_news`)**
        *   **具体区域/版块：**
            *   快讯来源/类型筛选标签页 (例如"精选"、"Twitter"、"博客"、"新闻"等)
            *   快讯时间流列表区 (包含时间、标题、简要内容、来源)
            *   事件日历与事件列表区

---

**B. 主要分组：DEX**
    *   **1. 页面：市场 (DEX) (`dex_market`)**
        *   **具体区域/版块：**
            *   **DEX行情列表视图:**
                *   顶部筛选与搜索区 (含代币搜索、链选择器、时间周期选择)
                *   DEX代币行情列表区 (含价格、成交额、涨跌幅、安全检测等关键信息)
            *   **DEX交易终端视图 (点击行情列表项后进入):**
                *   交易对与实时数据展示区
                *   专业K线图表区 (例如TradingView风格)
                *   近期成交历史列表区
                *   订单输入面板 (含价格/数量输入、买卖操作、订单类型选择、滑点设置)
                *   用户仓位与订单管理区 (含消息追踪、持仓信息、市价委托单、历史委托单等)
    *   **2. 页面：Trackers (`dex_trackers`)**
        *   **具体区域/版块：**
            *   Trackers功能头部 (含KOL/项目来源切换、新建订阅按钮)
            *   智能化追踪信息流区 (每条信息含原文链接、互动数据统计、AI智能分析模块如情感分析/相关话题等)
            *   动态多功能侧边栏 (可按需展示：
                *   合约交易面板
                *   刷刀策略面板 (含手动模式和自动模式配置/执行)
                *   实时通知中心
                *   自动化策略创建表单等)
            *   创建新订阅配置模态框 (包含订阅名称、描述、触发关键词等)
    *   **3. 页面：Farcaster (`dex_farcaster`)**
        *   **具体区域/版块：**
            *   KOL发现与推荐区 (例如"追踪KOL的最新动态"标题、推荐KOL卡片列表、引导关注入口)
            *   Farcaster Casts (帖子) 信息流区 (展示Farcaster帖子内容与互动；用户关注KOL后，此信息流可能与CEX关注列表中的KOL动态部分内容相似)
            *   已关注KOL列表侧边栏 (可管理关注的KOL)
            *   KOL追踪代币动态列表侧边栏 (展示KOL关注或提及的代币动态)

---

**C. 主要分组：通用**
    *   **1. 页面：关于我们 (`common_about_us`)**
        *   **具体区域/版块：** (基于落地页内容)
            *   核心使命与愿景区 (例如 "BBX mission is to social trading data platform")
            *   成就数据展示区 (例如统计数据：100+ Exchanges, 10000+ Coins等)
            *   公司故事与价值观区 (包含公司背景、理念如"All is FREE"等)
            *   客户评价/推荐区
            *   联系方式入口/客户服务区 (例如提及邮件联系方式)
            *   (可能包含)团队介绍/加入我们入口
    *   **2. 页面：联系我们 (`common_contact_us`)**
        *   **具体区域/版块：**
            *   页面标题与引导文字区
            *   社交媒体联系入口区 (例如Twitter, Telegram链接按钮)
            *   邮件联系表单区 (包含标题、有效邮箱、留言内容输入框及提交按钮)
            *   (可能存在)常见问题(FAQ)链接区
    *   **3. 页面：积分 (`common_points`)**
        *   **具体区域/版块：**
            *   顶部用户积分总览区 (显示用户当前总积分)
            *   邀请好友功能区 (包含邀请链接、成功邀请计数、已赚取积分统计)
            *   连接社交账号赚取积分区 (例如Twitter, Telegram, Farcaster，显示连接状态和操作)
            *   完成日常任务赚取积分区 (例如免费上币请求、推特点赞、每日登录、添加自选币、联系我们等任务卡片)
            *   积分/邀请排行榜切换区 (含"积分排行榜"和"我的邀请"两个标签页，分别展示对应列表)
            *   每日积分上限提示区
    *   **4. 页面：手续费返佣 (`common_commission_rebate`)**
        *   **具体区域/版块：**
            *   手续费活动Banner区 (例如"限时免交易手续费")
            *   邀请好友功能区 (手续费返佣相关，包含邀请链接、成功邀请计数、已赚取统计)
            *   我的邀请列表及返佣统计区 (显示被邀请人信息、层级、交易量及产生的返佣)
            *   (可能存在于其他位置或特定条件下)当前费率等级显示区
            *   (可能存在于其他位置或特定条件下)详细费率表/说明区
    *   **5. 页面：个人中心设置 (`common_user_profile`)**
        *   **具体区域/版块：**
            *   页面标题区 (例如"我的账号")
            *   头像更改功能区 (包含当前头像展示和更改入口)
            *   用户名编辑功能区 (包含当前用户名展示和编辑入口，可能含修改次数限制提示)
            *   邮箱展示区 (通常不可编辑)
            *   社交账号关联状态与管理区 (例如Twitter, Telegram, Farcaster的关联状态及操作入口)
            *   (通常包含但截图中未直接显示)安全设置区 (例如修改密码、两步验证/2FA、API Key管理入口)
            *   (通常包含但截图中未直接显示)身份认证(KYC)状态/入口区
            *   (通常包含但截图中未直接显示)偏好设置区 (例如语言、主题颜色、通知偏好设置入口)

---

**D. 其他重要但非表单主要选项的页面/模块 (简述)**
    *   **认证模块 (`(auth)/`)**: 包括登录、注册、忘记密码等页面。这些是标准功能，通常由特定组件处理。
    *   **全局组件**: 如顶部导航栏、页脚、全局通知等，这些元素可能跨越多个页面，修改时需指明具体是哪个全局组件。
    *   **特定流程页面**: 如邀请落地页、账户激活页等。
    *   **交易核心模块 (Orderly)**: `(orderly)/` 下的复杂交易界面，虽然部分功能点（如合约交易）已提取到CEX Market，但整体交易界面是一个高度集成的模块。
    *   **通用内容页面**: 如隐私政策、服务条款、具体的新闻/文章详情页（区别于"快讯"列表）。
    *   **移动端特定视图**: 项目中可能存在 `/mobile/` 路径下的特定视图，这些是为移动设备优化的，但表单主要关注核心桌面端导航。
    *   **错误和未找到页面**: 标准的 `404.tsx` 和 `error.tsx`。

---

**E. (兜底选项) - 与旧结构保持兼容，供参考**
    *   **1. 主要页面子项：其他未列出的特定页面**
        *   **具体区域/版块：** 请您描述其主要功能和在导航中的位置
    *   **2. 具体区域/版块：其他未列出的特定区域**
        *   **具体区域/版块：** 请您描述其在选定页面中的位置和特征 